# Resource-Monitor
simple cross-platform (windows &amp; linux) resource monitor to quickly see how system is performing

# System Resource Tracker

Monitors CPU, memory, disk, and network usage. Works on Windows and Linux.

## Usage

```bash
pip install psutil
python resource_tracker.py
```

Updates every 5 seconds. Press Ctrl+C to stop.
